WeensyOS is a tiny kernel that can run on bare-metal x86-64 machines (for lab4, QEMU's emulated CPUs), developed by Eddie Kohler. The initial state of the kernel contains code for bootstrapping kernel, handling exceptions/syscalls, executing user-level program, and helper functions for your Project 4/5 exercises.
